import loginApi from "../../../api/login/login";
import cookies from "vue-cookies";
const state = {
  on_or_up: "on"
};
const mutations = {
  onOrup(state, payload) {}
};
const actions = {};
export default {
  state,
  mutations,
  actions
};
